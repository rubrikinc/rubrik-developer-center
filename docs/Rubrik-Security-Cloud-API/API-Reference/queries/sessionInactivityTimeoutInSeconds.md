# sessionInactivityTimeoutInSeconds

The session inactivity timeout in seconds.

## Returns

[Long](../types/scalars/Long.md)!

## Sample

=== "Query"

    ```graphql
    query {
      sessionInactivityTimeoutInSeconds
    }
    ```

=== "Variables"

    ```json
    {}
    ```

=== "Example Response"

    ```json
    {
      "data": {
        "sessionInactivityTimeoutInSeconds": 0
      }
    }
    ```
